export const FileType = {
    TEXT: 'txt',
    JSON: 'json'
}

export const FileTypeStrategy = {
    txt: FileType.TEXT,
    json: FileType.JSON
}

export const FileBasePath = {
    FIXTURES: 'cypress/fixtures',
    DOWNLOADS: 'cypress/downloads',
    REQUESTS: 'cypress/requests'
}